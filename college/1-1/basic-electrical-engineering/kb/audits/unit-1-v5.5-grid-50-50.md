# Unit I v5.5 — 50/50 Grid adjustment

- All side-by-side textbook Grid rows now use equal text/image columns: `minmax(0, 1fr) minmax(0, 1fr)`.
- Full-width figure rows are unchanged.
- Mobile behaviour remains single-column below 720 px.
- Resistivity remains corrected as `\(\rho\)` in both `topics.json` and `unit-1.md`.
- BEE KB verifier: PASS.
- General college verifier still reports only the three pre-existing Unit II meta-commentary warnings.
